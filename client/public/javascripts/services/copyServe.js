app.factory('copyServe',['$http', 'eventServe', 'taskServe', 'shiftServe',
	function($http, eventServe, taskServe, shiftServe){

		return {
			copyEvent: function(eventId, newStartAt, newEndAt) {

			}
		};

	}]);
